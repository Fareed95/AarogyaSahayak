services:

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: arogya_sahayak_server_api
    volumes:
      - ./server:/app
    ports:
      - "8000:8000"
    networks:
      - templet


networks:
  templet:
    driver: bridge